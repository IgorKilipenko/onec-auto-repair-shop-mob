#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаОповещения(Знач имяСобытия, _, __)
    Если имяСобытия = "НоваяЗаписьКлиента" Тогда
        ЭтотОбъект.Элементы.Список.Обновить();
    КонецЕсли;
КонецПроцедуры

#КонецОбласти // ОбработчикиСобытий